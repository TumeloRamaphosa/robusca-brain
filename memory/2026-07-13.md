# Session Log — 2026-07-13
**Agent:** Robusca (Cursor Cloud)
**Agent Lord:** Tumelo Ramaphosa

## Task
SSH to `robot@45.61.56.91` and install Composio (`composio-core` / `composio`).

## Outcome
- **BLOCKED on SSH:** Host reachable; auth failed `Permission denied (publickey,password)`. No private key for this host in the cloud agent environment.
- Generated ephemeral ed25519 key for this agent (pub in `~/.ssh/id_ed25519.pub`) — Tumelo must add it to the VM `authorized_keys`, OR vault/provide the existing VM SSH key.
- **Local fallback (this cloud agent box only):** `pip install --user composio` → `composio 0.17.1` OK. npm global install attempted; prefix fixed to nvm.
- Target VM install **not done** until SSH works.

## Architecture captured (Cipher Tr@ce mesh)
Tumelo clarified the target stack:
- **Composio** = action layer (GitHub `dark-factory`, Notion CRM, Slack `#sales`, Stripe, Google Sheets) — AI intents, no manual OAuth per call
- **Channels:** WhatsApp→RileyJarvis (Mac Mini), Discord→discli+Discord.js, Telegram Bot API, Whisper STT in, Kokoro/Voicebox out, text→Cipher Tr@ce
- **Flow:** `RileyJarvis → Ollama (Qwen3) → robusca-brain → N8N → Notion`
- Spec written: `COMPOSIO_MESH.md`; linked from `TOOLS.md`, `STUDEX_OS.md`, `IDENTITY.md`

## Mac Mini tonight playbook
Tumelo pasted install steps. Captured + corrected in `MAC_MINI_TONIGHT.md`:
- RileyJarvis clone OK (`rbrown101010/rileyjarvis`, `npm run dev` → WhatsApp QR)
- Kokoro: `remsky/Kokoro-ONNX` **404** → use `remsky/Kokoro-FastAPI` on port **8880** (or `kokoro-onnx` lib)
- Whisper: `pip install openai-whisper` OK
- Discord: bare `discli` npm **unpublished** → `@ibbybuilds/discli` + `discli init --token` (not `discli auth`)
- Composio intents restated (GitHub/Notion/Slack/Stripe/Sheets)
- Added `scripts/mac-mini-tonight.sh` (one-shot installer) + `scripts/composio-smoke.py`
- Pubkey for VM auth: `deployment/CLOUD_AGENT_SSH_PUBKEY.md` (SSH still denied)

## Install push (continue)
Local cloud box now has:
- `@composio/core` + Python `composio` + Whisper + `@ibbybuilds/discli`
- RileyJarvis cloned + `npm install` under `.agents/rileyjarvis` (gitignored)
- Ready script: `scripts/vm-composio-install.sh` (fails until SSH authorized)
- No Docker here → Kokoro container must run on Mac Mini
- VM SSH still `Permission denied`

## ClawX voice (Kokoro + Whisper)
New paste for Mac Mini ClawX Settings → Voice. Spec: `CLAWX_VOICE_MAC_MINI.md`.
- Still: `remsky/Kokoro-ONNX` + `kokoro_server.py --port 5002` = **dead**
- Use Kokoro-FastAPI `:8880`; ClawX TTS/STT = Kokoro + Whisper
- Prefer `uv pip install` inside ClawX (per TOOLS.md)

## Next
1. Tumelo on Mac Mini: run ClawX voice steps in `CLAWX_VOICE_MAC_MINI.md`
2. Authorize pubkey in `deployment/CLOUD_AGENT_SSH_PUBKEY.md` on the VM — then say "go"
3. Connect Composio apps once (after rotating burned key — KEY_ROTATION #8)
4. Optional: Sent.dm via `SENT_COMPOSIO.md`

## Security — Composio key in chat
Tumelo pasted a live `COMPOSIO_API_KEY` into chat (ends `...RPEo7f`). **Not written to any file.**
Key treated as burned → KEY_ROTATION_CHECKLIST item 8. Scaffold: `.env.example`, `SENT_COMPOSIO.md`, Sent demo ref at `.agents/sent-dm-demo`.
