# 2026-07-13

## Session notes

- Owner asked how to add MiniMax + Sakana Fugu to Cursor, plus Cursor API + MCP — answered with BYOK override path, dashboard API keys, and MCP config.
- Owner pasted three items: a Google/NotebookLM-looking token, a NotebookLM notebook URL, and `kepano/obsidian-skills`.
- **Security:** Token was NOT written to disk or env. Flagged in `KEY_ROTATION_CHECKLIST.md` item #8 — rotate; never paste secrets in chat.
- Registered NotebookLM URL in `notebooklm/README.md` (topic TBD).
- Vetted and installed kepano Obsidian skills into `skills/`: obsidian-cli, obsidian-markdown, obsidian-bases, json-canvas, defuddle. No exfil / destructive patterns found.
- Owner clarified intent: connect an agent to NotebookLM (why the token + notebook URL were sent).
- Wired NotebookLM MCP: installed `notebooklm-mcp-cli`, Cursor MCP at `~/.config/cursor/mcp.json` + project `.cursor/mcp.json`, skill `skills/studex-notebooklm`, docs `notebooklm/AGENT_CONNECTION.md`.
- Auth blocker: lone `AQ.*` token is not a full cookie header — cannot complete live NotebookLM auth without `nlm login` (browser) or full `SID=…; HSID=…` cookie string via env (not chat/git).
