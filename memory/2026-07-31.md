# 2026-07-31

- CI hygiene: added `environment.yml`, `tests/test_smoke.py`, and pytest `--suppress-no-test-exit-code` so Conda + Python package workflows stop failing on missing env file / empty suite.
- Tumelo asked how to connect OpenClaw to collaborate with this Cursor Cloud agent — answer via ACP (`/acp spawn cursor`) + shared git/memory; OpenClaw cannot join this Cloud session directly.
